services:
  devdb:
    build: { context: .., dockerfile: docker/Dockerfile }
    image: devdb:dev
    init: true
    ports:
      - "127.0.0.1:4400:4400"
      - "127.0.0.1:54300-54339:54300-54339"
    volumes:
      - devdb-data:/data
    stop_grace_period: 60s
volumes:
  devdb-data:
