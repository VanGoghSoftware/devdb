# DevDB — Phases 2–5 Handover

**Date:** 2026-07-03 · **Audience:** the agent (or human) picking up DevDB after phase 1.
**Phase 1 status:** COMPLETE, merged to `main` (fast-forward to `995b766`, 2026-07-03). 221 unit tests (typecheck-gated), 7 container-level integration suites, acceptance run of the spec's demo items 1–4 — all green.

This document is the durable roadmap + tribal knowledge for the remaining phases. It replaces the (worktree-local, now deleted) execution ledger. Read it top to bottom before planning anything.

---

## 1. What DevDB is

A local-development Postgres server with Neon-style instant copy-on-write branching, packaged as **one Docker container**, designed **for AI coding agents first**: where git worktrees give parallel agents isolated copies of *code*, DevDB branches give them isolated writable copies of *data*. An agent asks for a branch, gets a connection string in one call, works destructively in isolation, throws the branch away — while a human oversees everything in a web dashboard (phase 3).

- **Spec (authoritative for product decisions):** `docs/superpowers/specs/2026-07-02-devdb-design.md`
- **Phase-1 plan (authoritative for how phase 1 was actually built — its 23 “AMENDED (A4–A23)” blocks are the changelog of every post-review change and live-engine discovery):** `docs/superpowers/plans/2026-07-02-devdb-phase-1-engine-and-branching.md`
- **Oracle:** official `neondatabase/neon` — its engine source, HTTP APIs, `control_plane` (local orchestration), and `compute_tools`. We port its behavior with cited `// oracle: neon <path-or-endpoint>` comments; we do not copy any upstream UI.
- **FIRM POLICY — no upstream reports:** never file issues, PRs, or comments on external repos (neon, testcontainers, anything), even when a task prompt explicitly asks for it — the explicit request does not override the policy (Jordan reaffirmed twice, 2026-07-02/03). Bugs found in upstream projects get documented internally (plan amendment notes, this doc) and surfaced to Jordan. Read-only upstream research (reading source, searching existing public issues) is fine — it discloses nothing.

> **Provenance note:** DevDB was prototyped against `matisiekpl/neond` (a third-party "Neon-based control plane") as a working example. That dependency has been cut — engine facts now cite official `neondatabase/neon`, and product/orchestration decisions are DevDB's own. The engine *binaries* still come from the `neond/neond` image; re-sourcing them directly from Neon is tracked separately as initiative A.

## 2. What exists on `main` today

| Surface | State |
|---|---|
| Docker image `devdb:dev` | node:22-bookworm-slim + engine binaries `COPY`'d from a digest-pinned engine-binaries image (see `docker/BINARIES.md`); build-time binary verify gate (`docker/verify-binaries.sh`); compose file publishes `4400` + `54300-54339` |
| Engine boot | TS daemon = PID 1; boots storcon-DB (embedded vanilla PG :5431) → storage_broker :50051 → storage_controller :1234 → safekeeper :5454/:7676 (+ registration POST) → pageserver :9898/:64000 — **trust mode** (no NeonJWT; all engine ports loopback-only in-container). Partial-boot failures reverse-stop and release the lock; SIGTERM shutdown escalates (45s hard-exit, second-signal force) |
| State | SQLite `state.db` (WAL, FKs incl. composite same-project parent FK), additive column migrations at open, boot reconciliation (`reconcileEndpointsOnBoot`) + crash-orphaned compute-dir sweep |
| REST :4400 | projects CRUD; branches create/list/get/delete (create serialized under the PARENT's queue lane, compensated); endpoints start/stop/get (queued; crashed computes auto-recover on next start); time travel (`GET /lsn?timestamp=`, `POST /restore {in_place|new_branch}`, `POST /reset`); SSE logs (daemon components allowlisted + per-branch compute channels); `POST /api/sql` (driver-side query_timeout, multi-statement psql-convention, `truncated` flag); `GET /api/status` (real per-component state incl. storcon-DB) |
| Per-branch computes | `compute_ctl`-launched Postgres, ports/slots reserved synchronously, sticky ports, durable `endpoint_error`, orphaned-postgres reaper (compute_ctl loses its child on SIGTERM — see §8) |
| Tests | `pnpm --filter @devdb/daemon test` (tsc gate then vitest, 221 tests); `pnpm --filter @devdb/integration test` (7 files: boot, projects, branching-isolation “money test”, endpoints incl. live stop/port-reuse, timetravel, restart, acceptance; ~5 min total; testcontainers with an in-repo port-cache-race workaround) |
| Review memory | `docs/codebase-review.md` — the review-broker's append-only prior-findings doc (checked in; dedup memory across phases). Keep pointing the broker at it: `REVIEW_BROKER_DOC=<repo>/docs/codebase-review.md` |

**Repo layout:** `packages/daemon` (src: `config.ts`, `state/*`, `engine/*`, `compute/*`, `services/*`, `http/api.ts`, `index.ts`), `packages/shared` (zod schemas + DTO types), `docker/`, `tests/integration/` (+ `helpers/pg.ts`, `helpers/container.ts`), `docs/`.

**Check `git log` before planning:** Jordan runs hands-on sessions in parallel (IDE + chip-spawned sessions — currently: compute_ctl ready-vs-auth investigation, the task-14 “500 on stop” revisit, and two upstream-report drafts). `main` may have moved past `995b766`. His direct fixes ride into commits with disclosure + plan amendment notes; that pattern is welcome — absorb, verify, credit.

## 3. Process contract (how phases get built)

Phase 1 was built with the superpowers flow and it worked well. Keep it:

1. **Each phase gets its own spec-refinement (light) + full implementation plan** via `superpowers:brainstorming` (only where genuine product questions remain — most were settled in the spec) and `superpowers:writing-plans` (bite-sized TDD tasks, complete code in steps, `Interfaces:` blocks, oracle citations). Do NOT try to execute a phase from this handover alone.
2. **Execution:** `superpowers:subagent-driven-development`. Per task: implementer subagent (haiku for transcription tasks, sonnet for live/judgment tasks) → **two parallel gates**: a task-reviewer subagent AND a review-broker scan → controller adjudicates merged findings → one fixer → focused re-review. Ledger every verdict in `.superpowers/sdd/progress.md` (worktree-local; gitignored — **copy anything durable into a committed doc before removing the worktree**; phase 1 lost its ledger this way and this handover is the reconstruction).
3. **Review broker:** user-scoped MCP (`mcp__review-broker__*` — available natively in new sessions). Per-task: `run_intelligent_review_scan` with `repoRoot` = the worktree, absolute `focusFiles`, `appendToDoc: true`, `targetFindings` 5–6, `priorityFloor` "P4", task-scoped `directions` that name the sanctioned deviations (so it doesn't burn findings on them). Severity map: P1–P2 → Critical, P3 → Important (fix now), P4–P5 → Minor (ledger, final-review triage). The broker runs on Jordan's Codex account — it costs real tokens; one scan per task + one per fix round only when warranted.
4. **Standing user rulings (still in force):**
   - **A1:** integration tests import shared helpers from `tests/integration/helpers/` — no per-file duplication.
   - **A2:** unit tests use typed fakes against the narrow interfaces in `services/engine-api.ts` — no `as never`/`as any`. The daemon test script's tsc gate enforces test-file types; keep it.
   - **Standing rule:** when a gate flags something the plan's literal text mandates and the controller judges the finding correct → fix it AND amend the plan (numbered `> **AMENDED (An, …)**` note), autonomously; only architecture-changing conflicts go back to Jordan.
   - **Supply chain:** `minimumReleaseAge: 1440` (npm deps ≥ 24h old), pnpm 11.9.0 via packageManager pin, `allowBuilds` allowlist (every new native dep needs an explicit true/false decision), image installs with `--frozen-lockfile` (remember: a new workspace importer requires COPYing its `package.json` into the image before `pnpm install`).
   - Conventional commits; `Co-Authored-By: Claude` on controller commits; worktree per phase (`git worktree add` under `.claude/worktrees/` — the native EnterWorktree tool may fail if the harness cached pre-git state; the manual + `EnterWorktree {path}` fallback also failed once, plain `git worktree add` + absolute paths worked fine).
5. **Machine quirks:** corepack's shim is broken under some node versions here — use plain `pnpm`. `core.autocrlf=true` is set globally; since 2026-07-03 the repo pins `* text=auto eol=lf` in `.gitattributes`, so fresh checkouts/worktrees get LF (before the pin, the CRLF smudge broke `docker build` at the verify-binaries gate: bash reads `set -euo pipefail\r` → “set: pipefail: invalid option name”). Worktrees created BEFORE that commit still hold CRLF on disk — merge/rebase the fix into the branch, then re-smudge from the worktree root (`git ls-files -z | xargs -0 rm -- && git checkout -- .`) or recreate the worktree. Docker Desktop runs many unrelated containers — first-run integration flakes happen under load; rerun before diagnosing.
6. **Verify a reported bug still exists on CURRENT main before fixing it.** Multiple sessions (agent + Jordan) commit to main in parallel; a task prompt, chip, or bug report is a snapshot, not a contract. Near-miss on 2026-07-03: a spawned task targeted the ComputeManager ENOTEMPTY race on a branch that main had already merged AND fixed with a stronger root cause (`reapOrphanedPostgres`) — landing the prompt's suggested fix as written would have regressed main. Before implementing: read the current version of the target file on main, skim `git log main` for supersession, and retarget the work to what main still lacks (often just tests, or the same bug in a sibling code path).

## 4. Phase 2 — MCP server + agent skills (COMPLETE 2026-07-03)

**Goal:** agents create/use/reset/restore branches without human help; skills establish the branch-per-task workflow. This is the phase that delivers the product's original pitch.

> **STATUS: COMPLETE (2026-07-03), branch `phase-2-mcp-and-skills`.** Built via subagent-driven-development (per-task: implementer → task-reviewer subagent + review-broker scan → controller adjudication → fixer; the concurrency/security-critical tasks got an extra attacker-mindset Fable re-review). All 6 §4-parked decisions resolved (lane capability tokens → turn-scoped branded `Lane`; DTO redaction + generic 409s; structured logging → `daemon:app`; `/metrics` readiness + statusOf race fix; process-group compute kill; MCP-concurrency integration test). MCP surface: session-stateful `/mcp` transport, a Fastify Host/Origin DNS-rebinding guard (the sole protection on the unauthenticated endpoint — a `%6dcp` decode-bypass was found and closed by gating on `req.routeOptions.url`), 10 tools, fork-context capture (session client merged in, spoof-safe), DTO-redacted actionable responses. Plus REST context parity, `skills/using-devdb` + `skills/safe-db-migrations`, loopback compose bind + README. **Verification:** unit 362/362 (29 files); full container integration 10 files / 13 tests / 0 fail (incl. MCP handshake/acceptance/concurrency + all phase-1 suites — no regression). **Whole-branch final review (Fable): Ready to merge — no Critical/Important cross-task defects.** Authoritative changelog: the `> AMENDED` blocks in `docs/superpowers/plans/2026-07-03-devdb-phase-2-mcp-and-skills.md` (every per-task deviation + the live-engine/SDK discoveries). Newly-deferred items pushed to §9.

### Scope (from the spec, §MCP + §Skills)
- **MCP server inside the daemon** at `http://localhost:4400/mcp`, Streamable HTTP, official `@modelcontextprotocol/sdk`. Registration UX: `claude mcp add --transport http devdb http://localhost:4400/mcp`.
- **Tools:** `list_projects`, `create_project {name, pgVersion?}`, `list_branches {project}` (tree + status + created_by + fork context), `create_branch {project, name, parent?, at_timestamp?, context?}` (**auto-starts endpoint, returns connection string** — the “new worktree” move), `get_branch {project, branch, ensure_running?=true}` (the “switch” move), `stop_endpoint`, `delete_branch`, `reset_branch`, `restore_branch {project, branch, to_timestamp, as_new_branch?, context?}`, `import_database` (STUB in phase 2 — real implementation is phase 4; return a clear “arrives in a later phase” error or defer the tool entirely — decide in the plan), `export_branch` (same), `get_job {id}` (same), `get_status`.
- **Fork context (spec amendment 2026-07-03 — Jordan):** branch-creating tools take `context {git_branch?, workdir?, agent?, purpose?}` — the caller's current git branch name, worktree path, agent/session label, one-line task description; the server adds `client {name, version}` captured from the MCP `initialize` handshake. Optional in the tool schema (bare calls still work) but described as strongly recommended; the skills fill the fields automatically (`git branch --show-current`, `$PWD`, task one-liner). Persist as a nullable `context` JSON column on `branches` (additive open-time migration — the T4 migration hook handles it), return it in branch DTOs (pairs with parked decision 2: context is UI-safe, `password` is not), and accept the same optional `context` on REST `POST /api/projects/:id/branches` for parity. MCP success responses also open with a context line naming the project/branch acted on (plus parent, for forks). Purpose: phase 3's branch tree shows whose fork is whose.
- **Design principles (spec):** every success response is actionable text including the connection string when relevant + a “next step” hint; every error names its remediation (`endpoint_error` is now durable — surface it); timestamps ISO-8601 with explicit timezone (the REST layer already rejects tz-less).
- **Skills** shipped in-repo under `skills/`, superpowers-conventioned, referencing MCP tool names exactly: `using-devdb` (branch-per-task discipline mirroring worktrees; naming `agent/<task-slug>`; wire connstring into the worktree's env; never share a branch between concurrent agents; delete on completion; always pass fork context on `create_branch`), `safe-db-migrations` (rehearse on a branch → verify → apply to main; `restore_branch` as the undo), `importing-databases` (phase-4-dependent — consider deferring this skill to phase 4 with the tools).

### Parked design decisions this phase's plan MUST resolve (accumulated from phase-1 gates)
1. **Lane capability tokens** — `startLocked`/`stopLocked` are public with a JSDoc “caller must hold the branch lane” contract; the final review recommended an enforceable shape (queue-issued token or `runLocked` helper). MCP adds more callers; do the refactor first.
2. **DTO mappers with redaction** — wire responses are currently `BranchRow` supersets: `password` is present on every branch response. Fine under localhost trust, wrong for MCP ergonomics and hygiene: map to explicit DTOs, drop `password` (agents get `connectionString`), genericize 409 constraint messages (they leak `branches.slug` style internals).
3. **Compute readiness probe** — the "listening on IPv4 address" needle fires when the postmaster starts listening, **~80–140ms before compute_ctl finishes `apply_spec`** (its `config_ms` startup metric). The race is a **first-ever-start problem**: until the `CreateAndAlterRoles` apply phase runs, the timeline still holds the PARENT branch's SCRAM verifier, so clients hit `28P01` (briefly `57P03`) in that window; second and later starts don't race because the verifier is durable in the timeline. Two bounded retries currently mask it (test helper + `/api/sql`'s `connectWithRetry`). **Structural fix (verified live 2026-07-02):** compute_ctl's external HTTP server (our metricsPort, 40000–40999) serves auth-exempt Prometheus `/metrics` with `compute_ctl_up{status="init|running|failed"}`, where `status="running"` is set strictly AFTER apply_spec commits (log: "Changing compute status from init to running"). Poll that in `ComputeManager.start` as the readiness gate, then delete the retries. Do NOT use the JSON `/status` endpoint: it validates a JWT against `compute_ctl_config.jwks`, we ship `{keys: []}`, so it is permanently 400 — and `--dev` does not bypass it.
4. **PDEATHSIG/setpgid port** — the oracle's `pre_exec` (Linux `PR_SET_PDEATHSIG`, macOS `setpgid`) was never ported to `ManagedProcess`; compute_ctl orphans its postgres child on SIGTERM, currently mitigated by `reapOrphanedPostgres` (path-scan + kill). Spawning computes into their own process group and killing the group is the structural fix (Node: `spawn(..., {detached: true})` + `process.kill(-pid)`); removes the reap's cross-file invariant dependence. Jordan has a session revisiting this — sync first.
5. **Structured logging** — compensation paths use bare `console.error`; route through a logger that also feeds the existing `LogsService` channels.
6. **MCP-specific concurrency review** — agents will hammer the API concurrently; the queue-lane model held up under review, but the plan should include a concurrency-focused integration test (parallel create/use/delete across branches — the product's core promise).

### Acceptance sketch (spec v1 items 3–4, MCP edition)
`claude mcp add … /mcp` → agent `create_branch` → gets connstring → destructive writes on its branch; `main` unaffected; `list_branches` shows the tree; `reset_branch` → branch matches parent again; `restore_branch --as-new-branch` recovers a pre-mistake timestamp. Ship at least one skill exercised end-to-end by a scripted agent flow in integration tests (MCP SDK client against `/mcp` inside the testcontainers harness).

## 5. Phase 3 — Web UI (design refined 2026-07-03 — read `docs/superpowers/specs/2026-07-03-devdb-phase-3-web-ui-design.md` first)

**Goal (spec §Web UI + the phase-3 refinement):** React 19 + Vite + **Mantine** (Jordan's explicit choice — not Tailwind/shadcn), static build embedded in the daemon at `:4400`, no login. Fresh design; do not copy any upstream dashboard UI.

- **Screens:** Dashboard (projects, engine health, static "local" storage chip until phase 4); **Project view = full-width branch tree + transient branch drawer**, top-bar shell. The tree ships **two switchable renderers** — git-graph rails (hand-rolled SVG; branches are a strict tree, no merges → DFS lane walk) and a **React Flow node canvas** (`@xyflow/react` + `d3-hierarchy`, nodes not draggable) — header toggle, default view in Settings (localStorage). Agent branches tagged via `created_by` + fork-context chip (agent, git branch; tooltip purpose/workdir/client). Branch drawer: connstring copy (DTO carries it; raw password field stays dropped), live SSE logs, restore picker (in-place w/ auto-stop notice vs as-new-branch), inline rename, Info tab (LSN/size/timeline), danger zone. Minimal Settings (read-only port range/storage/version + client prefs + phase-4 stubs). **SQL console deferred out of phase 3 entirely** (2026-07-03, Jordan — no SQL-client ambitions for now; see §9 for where its carried items went).
- **Freshness:** new **`GET /api/events`** SSE channel — coarse invalidation hints (`project.*`, `branch.*`, `endpoint.status`, `engine.health`), **no replay**, client = React Query invalidation + blanket refetch on every (re)connect. Emission post-local-write inside queue lanes; async compute transitions via a `ComputeManager.onStatusChange` hook wired in `index.ts` (compute/ keeps zero services/ imports).
- **Deferred REST from phase 1 landing here:** `PATCH /api/branches/:id` (rename) — create-name validation reuse, branch queue lane, 409 on duplicate, `slug` immutable, root branch not renameable (400).
- **Status payload additions:** `portRange {min,max}` + `storage "none"|"s3"|"azure"` (hardcoded "none" until phase 4).
- **Embedding:** `@fastify/static` v8 at `/` + SPA fallback that must never shadow `/api`/`/mcp`; Dockerfile web build stage; image `pnpm install` COPY set gains `packages/web/package.json` (§3.4 trap); dev mode = vite proxy to `:4400` (mind SSE buffering through the proxy).
- SSE consumption: auto-reconnect with capped backoff; events are hints, REST is truth (logs SSE keeps its replay; events deliberately have none).

**COMPLETE 2026-07-04** (branch `phase-3-web-ui`, 32 commits, per-task two-gate + Fable whole-branch final review). `packages/web` React 19 + Vite 8 + Mantine 9 + React Query 5 SPA; daemon delta (`/api/events`, emission at every mutation seam, ManagedProcess/ComputeManager/EngineRuntime observer hooks, `PATCH` rename, status `portRange`/`storage`, §9 dto typing closed, `@fastify/static` + hardened SPA fallback). 423 daemon unit + 129 web unit + 17 container-integration green. Web-test-harness contract (matchMedia/ResizeObserver mocks, `expect.extend` for jest-dom × vitest-4, `NODE_OPTIONS=--no-experimental-webstorage` for Node ≥24, `afterEach(cleanup)`, theme single-source via `THEME_STORAGE_KEY`) lives in `packages/web/test/{setup.ts,vitest.d.ts,render.tsx}`.

**Final-review follow-ups (none block merge; recorded so they're deliberate, not lore):**
- **Engine auto-restart with backoff was orphaned between phase plans.** Phase-1 plan deferred "supervised backoff-restart of crashed engine processes" saying it "arrives with the UI banner work in phase 3"; phase-3's spec then pinned "engine interactions — zero", so it was never built. The phase-3 degraded banner originally *claimed* auto-restart existed — corrected 2026-07-04 to name the real remediation (`docker compose restart devdb`). The feature itself (parent spec §Error handling: "Engine crash → supervised restart with backoff") is UNIMPLEMENTED — re-home it to phase 4/5 engine-supervision work; until then the banner tells the operator to restart the container.
- **Spec §Web UI items silently dropped from the plan (deliberate deferrals):** (a) the top-bar **project switcher** (Decision-4 shell list) — nav is dashboard-cards only; (b) **branch/running counts** on project cards; (c) kebab omits *restore*/*rename* (both reachable via the drawer's "Open panel"); canvas nodes carry no kebab (plan-pinned). Add to a future UI polish pass or amend the spec.
- **Minor UX seams (Fable final review):** in-place restore mints a NEW branch id (restoreSwap) so an open drawer keyed on the old `?branch=` id flips to the archived row — on restore success, re-point `?branch=` at the response DTO's id or close the drawer; `ProjectPage` `creating` + `CreateProjectModal` `name` don't reset on project change/reopen (4th instance of the reset-on-retarget class — daemon-guarded, UX-only); LogsTab re-appends replayed history on reconnect (bounded by MAX_LINES); a drawer on a branch deleted by another actor shows an indefinite skeleton (no "gone" affordance); no catch-all SPA route → a garbage deep link renders React Router's default error, not the friendly-404→dashboard.
- **Deferred-Minors triage (Fable): all ok-to-defer** — the §9 ManagedProcess stop-during-start clobber (pre-existing, lane-serialized, phase-3 preserved semantics); `sseStream` backpressure test (path moved verbatim from phase-1-hardened `sse()`); rename queued-race test (re-check is in-lane); `satisfies StatusDto` (root cause: `StatusDto.engine` union omits `"starting"` while `EngineRuntime.status()` returns `state: string` — widen the union on the next daemon touch, no consumer breaks today); web test-isolation flake (1/6, NEVER reproduced across 8 consecutive full-suite runs — the `clearMocks: true` hardening is the right standalone follow-up, not a blocker).
  - **AMENDED 2026-07-04 — that flake is reproduced, root-caused, and FIXED; it was NOT test-isolation/mock state.** The failing `findByRole("menuitem")` (BranchActionsMenu delete-dismiss test, and the same class in project-page's menu) times out because the OPEN Mantine Menu dropdown is painted `display: none` — items are mounted with `opacity:1` and a resolved position but drop out of the a11y tree. Source: Floating UI's `hide()` middleware measures the trigger via `getBoundingClientRect`, which jsdom reports as an all-zero rect, so it intermittently flags the reference "hidden"; Mantine renders that as `display:none` (`PopoverDropdown.mjs`, gated by `hideDetached && env !== "test" ? hide.referenceHidden : false` in `Popover.mjs`). It only fires under full-suite CPU load (~9 forks / 10 cores) — the flake is timing on the Floating UI `autoUpdate`, not mock accumulation (the failing case is the FIRST `it()`; `afterEach(cleanup)` runs; `pool:forks`+`isolate:true` gives each file a fresh process, so no cross-file leak is even possible). **Fix (`packages/web/test/render.tsx`):** a test-only theme default `Popover.extend({ defaultProps: { hideDetached: false } })`, which every dropdown component (Menu/Select/Combobox/HoverCard) inherits → `referenceHidden:false` → no `display:none`. Deliberately NOT `MantineProvider env="test"`: that also makes transitions *synchronous*, which breaks the two `waitForElementToBeRemoved()` modal-close assertions (dashboard.test.tsx:110, project-page.test.tsx:117) — verified it turns the 1/6 flake into a 3/3 deterministic failure. `clearMocks: true` is unrelated to this flake (it clears call history, not visibility) — still fine as separate hygiene, but the file's `.at(-1)` assertions would then need simplifying. Verified: 0/12 full-suite runs failed under amplified load (a throwaway 40-open probe added as the load generator), 3/3 on the real `pnpm --filter @devdb/web test`.

## 6. Phase 4 — Import/Export + Durability

**Goal (spec §Import & Export, §Durability):** the bucket features.

- **Import** (whole-DB → new branch, or seeds `main`). The empty-branch step is oracle: neon pageserver `TimelineCreateRequestMode::Bootstrap` (`pageserver/src/http/routes.rs`) — create empty branch (Bootstrap timeline) → start endpoint → `pg_restore` → job log channel (LogsService channels + `jobs` table exist, dormant since T4) → `importing → ready|failed` with stderr tail preserved. The SQL-level `pg_restore` flow itself is DevDB's own product choice (distinct from neon's binary-level `pageserver/src/import_datadir.rs`, which imports a raw datadir + WAL directly into a timeline rather than restoring via SQL against a running endpoint). **THREE source kinds (spec amendment 2026-07-04, Jordan — symmetric with the export destinations, one shared restore engine):**
  1. **Running server** — `pg_dump -Fc <source-connstring> | pg_restore` (`host.docker.internal` for host DBs). MCP `import_database {source_connection_string}` (+ UI form).
  2. **Uploaded `pg_dump -Fc` file — the INITIAL manual-restore path.** Web UI drag-drop + a REST **multipart** endpoint; `pg_restore` the uploaded artifact. NOT an MCP tool (agents don't upload files — this is a human/UI path). Closes the asymmetry that Export already emits `pg_dump` files but Import couldn't consume one. Watch: streaming a large multipart upload into `pg_restore` without buffering the whole file to disk; size limits; the same `importing → ready|failed` job + stderr-tail contract.
  3. **Configured-bucket artifact (S3 / Azure Blob) — FOLLOW-ON.** Pull a `pg_dump` from the same bucket the §Durability + `export_targets` config defines → `pg_restore`. Natural MCP-tool extension (a bucket source ref). Depends on the durability bucket wiring below.
  Build order within the phase: (1) base import, (2) file-upload (no bucket dep), (3) bucket-import after the durability config. All three + Export share ONE `pg_restore`/custom-format path — factor it once.
- **Export:** `pg_dump -Fc` from the branch endpoint streamed WITHOUT disk buffering to: local file (data dir), S3 multipart, Azure block blob. `export_targets` table exists (dormant). Job records size + LSN at dump time. Phase-2 MCP stubs (`import_database`, `export_branch`, `get_job`) become real here.
- **Durability:** daemon-level mode `none|s3|azure` — generate pageserver `remote_storage` (upstream Neon supports S3, Azure, GCS natively — verified in `libs/remote_storage/src/config.rs`; DevDB wires S3 + Azure) + safekeeper WAL backup args; `state.db` uploaded on interval + graceful shutdown; boot-time restore when local state missing; `none → bucket` switch allowed, reverse blocked with explanation. Surface the “all in sync” checkpoint badge in `/api/status` (+ UI).
- **Boot reconciliation, part 2:** diff engine timelines against SQLite at boot — this is the designated home for: the detach→swap crash-window orphan cleanup (documented at `timetravel.ts` swap call site), compensation residue (orphaned tenants/timelines when best-effort cleanup failed), and ghost-branch detection after a bucket restore (spec's backups doc warns about this).
- **S3 lease/lockfile:** if/when two-host safety is wanted — spec keeps it single-host; the local `.lock` + documented recovery command may suffice; decide in the plan. Note: official Neon's own answer to this class of problem (split-brain on shared remote storage) is storage-controller-issued **generation numbers** (`docs/rfcs/025-generation-numbers.md`), not an S3 conditional-put lock — a different mechanism, not a direct port, should DevDB ever need multi-host safety.
- **Tests:** MinIO + Azurite containers in the integration harness; disaster-recovery boot-from-bucket test (spec acceptance item 7); export artifact restored into vanilla Postgres (item 6).

## 7. Phase 5 — Extensions, PG 18, platform

- **Extensions (spec §Postgres versions & extensions):** contrib + pgvector already ship in the binaries. Add **pg_cron** and **PostGIS**, compiled per supported PG major against the shipped `pg_install/<ver>` headers (`pg_config` available in-image) in a Dockerfile stage. `shared_preload_libraries` currently = `neon` only (`compute/pgconf.ts`) — pg_cron needs appending + `cron.database_name` decided (computes are single-db `postgres` today). UI lists available extensions (phase 3 settings hook). Watch image size (PostGIS is heavy — consider an opt-in variant per the spec's risk note).
- **PG 18:** absent from the pinned compute image (inventory: v14–v17 + vanilla_v17 — note vanilla reports `19devel`, internal-only, excluded from tenant versions). Options: re-pin a newer digest and re-run the inventory decision in `docker/BINARIES.md` + bump `SUPPORTED_PG_VERSIONS`/`PgVersionSchema`, or build v18 compute from the `neon` submodule (heavy). Spec default stays 17 until then.
- **Multi-stage image (deferred from phase 3, T15):** the Dockerfile is single-stage `devdb:dev` — it installs ALL workspace deps (incl. the web build tooling, vite/tsc) and builds in the runtime layer, so build tooling + workspace sources ship in the final image. Phase 3's web build added only ~150 MB (image ~2.6 GB, dominated by the neon binaries — under the 300 MB restructure threshold), so it was left as-is. A multi-stage builder (build web/daemon dist in a builder stage, copy only `packages/*/dist` + the daemon's prod `node_modules` + neon binaries into a slim runtime) is the CI/image optimization to do here — also cuts the runtime dependency attack surface. Broker-flagged P4 at T15.

**Beyond phase 5 — phase 6 candidate (PARKED): DevDB Hub.** Self-hosted registry where teams share complete DBs and forks between DevDB instances ("Docker Hub for databases"): repos → immutable content-addressed `pg_dump` versions → movable tags, fork lineage as a parent-pointer DAG, client-side provenance closing the pull→fork→push loop, one-team token auth. Approved design spec: `docs/superpowers/specs/2026-07-03-devdb-hub-design.md` (2026-07-03). Hard dependency on phase 4's import/export pipelines; rides phase-2 fork context and phase-3 UI kit. Not scheduled — when Jordan green-lights it, write its plan fresh from that spec (its stage 1, golden datasets, is the v1 slice).
- **Platform:** multi-arch images (inventory verified linux/arm64 only — documented caveat in BINARIES.md), CI for unit + integration (the testcontainers workaround makes integration CI-viable; unique image tags per worktree/CI job was a consciously deferred finding), **testcontainers upgrade to ≥11.4.0** (upstream fixed both race paths — see §8.9; migrate, re-run the T16 repro, then delete our workaround), `.claude`/skills packaging decisions, quickstart polish.

## 8. Tribal knowledge — live-engine facts (do not re-learn these the hard way)

All of these are also recorded as plan amendment notes (A4–A23) with commit references:

1. **storcon `tenant_create` wants config fields FLAT on the body** — nesting under `config` → 400 “unknown field”. (A17)
2. **~5s post-boot window** where storcon 409s tenant creation (“No pageserver found matching constraint”) while the pageserver heartbeats warming-up→active; `/api/status` can't see it. Bounded retry exists in `storcon-client.ts`. A metrics/scheduler-readiness gate is the structural alternative if it ever bites elsewhere. (A17)
3. **compute_ctl requires `compute_ctl_config: { jwks: { keys: [] } }`** even in trust mode (serde demands the struct). (A19)
4. **Postgres GUC values with dots need quoting** — unquoted `listen_addresses=0.0.0.0` is a conf parse error; `pgQuote()` exists in `compute/pgconf.ts`. (A19)
5. **`get_lsn_by_timestamp` returns `kind:"future"` until LATER COMMITS advance the timeline clock** — wall-clock waiting never resolves it; integration tests poll AFTER the next write. (A21)
6. **compute_ctl orphans its postgres child on SIGTERM** (we never ported the oracle's PDEATHSIG/setpgid `pre_exec`); `reapOrphanedPostgres` mitigates; process-group kill is the structural fix (phase 2 §4.4). This was the true cause of a “500 on stop” initially misread as environmental. (A21/T15)
7. **compute_ctl readiness:** the stderr needle fires ~80–140ms before `apply_spec` completes; the auth gap only bites a branch's FIRST-ever start (parent's SCRAM verifier persists in the timeline until `CreateAndAlterRoles`). Reliable probe: auth-exempt `/metrics` → `compute_ctl_up{status="running"}` (set strictly post-apply_spec). `/status` requires JWT (empty jwks → permanently 400; `--dev` doesn't bypass). Full detail in §4.3.
8. **`--internal-http-port` must be allocated per compute** — default 3081 collides across concurrent computes (Jordan found this via `/proc/net/tcp`; fixed in `ComputeManager`). (A20)
9. **testcontainers@10.28.0 has a port-binding cache race** on start()/restart() with many exposed ports — worked around in `tests/integration/helpers/container.ts` (Devdb owns its port cache; retryStart stops failed live attempts). (A22) **Upstream research (2026-07-03):** fixed upstream — the start() path in testcontainers **11.0.1** (issue #818 / PR #1032, June 2025) and the restart() path in **11.4.0** (PR #1087 “Wait for ports to be bound when container restarts”, 2025-07-23) via `inspectContainerUntilPortsExposed` (250ms polls until every port has a non-empty host binding). Latest is v12.0.4 (2026-06-29). Upgrading to ≥11.4.0 makes our workaround redundant — it's a major-version migration, so re-run the T16 isolation repro before deleting the workaround. Slotted as a Phase 5 platform task.
10. **Upstream findings (kept internal):** compute pg_hba's inherited default uses `::1/32` (vastly broader than IPv6 loopback; we ship `/128`) (A15); the orphaning in #6 is DevDB's own process-launch gap — official Neon's `compute_ctl` has no PDEATHSIG/setpgid handling for its postgres child either (verified: no `pre_exec`/`setpgid` in `compute_tools/src/`), so there is no upstream mechanism to port here; `reapOrphanedPostgres` is DevDB's own mitigation, and process-group spawn+kill (tracked separately) would be DevDB's own structural fix.
11. **`format_version` serializes as JSON `1`** (JS int) — live-confirmed fine for compute_ctl's deserializer. (A15/A19)
12. **Stale lock recovery:** `docker compose -f docker/compose.yaml run --rm devdb rm /data/.lock` (README Troubleshooting; only if no other container uses the volume).
13. **Engine reserved ports:** 50051, 1234, 5431, 9898, 64000, 5454, 7676 (+ 4318 referenced by storcon's `--control-plane-url`, + computes' 40000-40999 metrics/internal range — the config validator covers the first seven; extending it to the other two is an open Minor).

## 9. Open Minors & consciously-declined items (final-review triage, reconstructed)

**Accepted as-is (phase 1):** verify-binaries ldd-absence edge; joined missing-env-var message; dead `id != newBranchId` guard in restoreSwap; SCRAM retry ≤1.5s masking window (server-issued passwords); `log_line_prefix` hand-quoted literal; format_version JSON int.

**Deferred with a home:**
- → Phase 2 **(DONE 2026-07-03)**: lane capability tokens (turn-scoped branded `Lane`); DTO redaction + genericized 409s; structured logging; PDEATHSIG port → process-group kill; metrics-based readiness (+ statusOf pre-readiness race fix); swap empty-lane micro-window closed. **STILL deferred:** the *stop-during-start interleaving* state-clobber in `ManagedProcess` (a late readiness needle after `stop()` sets state back to "running") — pre-existing, only reachable outside the lane discipline; a targeted `ManagedProcess` hardening + test remains open.
- → With the SQL console (**deferred beyond phase 3**, unscheduled — 2026-07-03 phase-3 refinement): SQL pagination/cursor & duplicate-column presentation. **Standalone daemon hardening, claimable any phase:** `sql.ts` materializes the FULL result set before applying the row cap — REST-reachable memory hazard regardless of UI; fix = stream and stop reading at cap+1.
- → Phase 3: README psql-race note only if reports come in. **(new, phase-2 final review):** `dto.ts` casts `endpointStatus`/`createdBy` from bare `string` — shared types are already union-typed on main (verified 2026-07-03); re-check the `services/dto.ts` mapper for residual casts and close either way.
- → Phase 4 (durability/boot reconciliation): detach→swap crash-window orphans; compensation residue; computesDir sweep already landed but engine-side orphan diffing pending. **port-allocation TOCTOU — FIXED 2026-07-03 (`16f30fc` + `827fba0`, reserve-then-probe).** `allocatePort` (`ports.ts`) *used to* resolve after its `tryBind` probe with the caller reserving *after* the await, so two genuinely-parallel branch starts (reachable via parallel MCP agents — the N=6 concurrency suite drives this) could pick the same port, both pass `tryBind`, and the second compute failed to bind. Now `allocatePort` claims each candidate into the shared `reservedPorts` set synchronously — via a `tryClaim` helper, *before* the probe's await — so interleaved starts can never be handed the same candidate (the claim lands atomically on the single-threaded event loop); the `finally` releases the claim on every non-grant path (probe returns `false` *or throws*). The three `ComputeManager.start` call sites dropped their now-redundant post-hoc `.add`. Covered by two deterministic concurrency unit tests (injected probe drives the interleave) + the mcp-concurrency integration suite. **Still deferred (Minor, review-broker P4):** a `ComputeManager`-level test proving `start()`'s failure cleanup releases *all three* claimed ports (endpoint/metrics/internal-HTTP), not just the endpoint port — the release lines (`manager.ts:195-197`) are traced-correct by two independent reviewers and are an identical symmetric idiom, so this is coverage debt, not a defect. Also: the P3 spawned during Task 1 (restore/reset `stopLocked`-before-try endpoint strand, `task_aeff72d6`) if not landed.
- → Phase 5/CI: arm64-only inventory; unique image tags per worktree/CI; reserved-port validator completeness (4318 + 40000-40999).

**Declined (do not re-litigate without a NEW argument — rationale in the phase-1 plan/reviews):** zod :id param validation; output-DTO zod schemas; Rust-source port-parity tests; mocked-execa EmbeddedPostgres contract tests; engine-client live-parity suite; needle fixture tests; lock-ownership nonce; boot-test live-engine probes; `rowMode:"array"`; cursor/streaming in phase 1.

## 10. How to start Phase 2 (concretely)

1. `git log --oneline -10` on main — absorb anything Jordan's parallel sessions landed since `995b766` (esp. the compute_ctl readiness + orphan investigations, which may pre-resolve §4.3/§4.4).
2. Read: the spec's §MCP + §Skills, this doc's §4, the phase-1 plan's A16/A19/A21/A22 amendment notes (ComputeManager/endpoints/timetravel current shapes), `services/engine-api.ts`, `services/endpoints.ts`, `http/api.ts`.
3. Brainstorm ONLY the genuinely open product questions (e.g., MCP tool auth posture on localhost, import/export tool stubbing vs omission, skill packaging/distribution) — everything else is settled.
4. Write the phase-2 plan with `superpowers:writing-plans` (same format: header, global constraints — carry §3.4's rulings verbatim — oracle map where applicable, bite-sized TDD tasks with Interfaces blocks). Include the parked-decision resolutions (§4) as early tasks — they're prerequisites for the MCP surface, not cleanup.
5. Execute with `superpowers:subagent-driven-development` + the review broker per §3. Create the worktree, initialize the ledger, and **at phase end copy durable ledger content into a committed doc before removing the worktree** (this doc exists because phase 1 didn't).
